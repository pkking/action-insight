[project]
name = "action-insight-ci-effective-report"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "openpyxl>=3.1",
  "psycopg[binary]>=3.2",
  "pyyaml>=6.0",
]

[tool.uv]
package = false
