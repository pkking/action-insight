# PostgreSQL connection string for the local Docker Compose database.
PG_DATABASE_URL=postgresql://action_insight:action_insight@localhost:5432/action_insight

# GitHub token for ETL collection and PR artifact rebuild.
GITHUB_TOKEN=

# Optional: retention window in days (default: 90).
RETENTION_DAYS=90

# Optional: verbose ETL logging.
VERBOSE=false
